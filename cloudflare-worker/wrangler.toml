name = "shabbos-rss-proxy"
main = "worker.js"
compatibility_date = "2024-01-01"

[vars]
# No environment variables needed — all config is in the worker

[[routes]]
# Will be auto-set after deployment to: shabbos-rss-proxy.<your-subdomain>.workers.dev
